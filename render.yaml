services:
  - type: web
    name: autogen-demo
    env: docker
    dockerfilePath: Dockerfile
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false
      - key: DATABASE_URL
        value: sqlite:////app/backend/autogen.db
      - key: PORT
        value: 10000
